pipeline {
    agent any

    parameters {
        string(name: 'LIB_BRANCH', defaultValue: 'main')
    }

    stages {
        stage('Audit tools') {
            steps {
                script {
                    // Load shared library dynamically
                    library("TestLib@${params.LIB_BRANCH}")

                    // Now call the step from the library
                    auditTools2("This is demo 2 from Job 2")
                }
            }
        }
    }
}
